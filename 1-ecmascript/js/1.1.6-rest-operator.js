const sum = (num1 = 0, num2 = 0, ...otherNumbers) => {
    console.log(`num1: ${num1}`);
    console.log(`num2: ${num2}`);
    console.log(`otherNumbers: ${otherNumbers}`);
    // resolver esta suma
}
  
sum(90, 10, [1,2,3,4]);